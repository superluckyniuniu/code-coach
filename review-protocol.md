# 校验协议

用户说「校验 / 交作业 / 帮我看」时执行。先读 WORKSHEET 清单，再读其代码。

## 步骤

1. **定位作业**：`practice/<topic>/` 或用户指定路径；同时打开 WORKSHEET。
2. **功能验收**：按「功能自测清单」逐条 → `通过` / `未通过` / `部分通过`（一句原因）。
3. **规范验收**：按「本练习相关规范点」+ 安全类全局强制项逐条 → 同上；违例写短名（如 `异常-禁裸except`）。
4. **Python+SQL**：按需对照 `python-mysql-format`（仅排版）。
5. **前端**：按需对照 `f2e-spec`。
6. **改法**：最多 1–3 条可执行提示（指向哪一段、缺什么）。默认不贴完整函数体。
7. **能力标签**：

```text
已掌握: …
需加强: …
```

标签宜具体：HTTP 与错误返回、环境变量、资源关闭、命名、SQL 参数化、分页批处理、日期时区、规范意识、脚本入口结构 …

8. **收尾三选一**：改完再交 / 看参考答案 / 换新需求出题。

## 输出骨架

```markdown
## 功能
- [通过/未通过/部分] 条目 — 说明

## 规范
- [通过/未通过/部分] 短名 — 说明

## 建议（1–3）
1. …

## 能力
- 已掌握: …
- 需加强: …

下一步：改完再交 / 看答案 / 新需求？
```

## 禁止

- 未要求「看答案」时贴整函数完整实现
- 用空泛夸奖代替清单
- 校验时覆盖重写用户未保存的作业文件
