# 教学文件模板（WORKSHEET.md）

生成练习时按下列章节输出。语言小白友好；**槽位内禁止写完整业务实现**。

---

## 1. 需求复述

用自己的话确认需求（3–6 句）。若有假设，列出来。

## 2. 整体思路

有序步骤（3–8 步）。写「真实脚本怎么做」，不要空概念。

## 3. 会用到的函数 / API / 库

每个条目固定格式：

### `name(...)`

- **干什么**：
- **关键参数**：
- **返回**：
- **怎么用（迷你例子）**：

```python
# 极简合法调用，不要夹带本题完整逻辑
```

- **易错点**：

## 4. 本练习相关规范点

从 `standards/` 选 3–8 条，写短名 + 一句话要求。例：

- `安全-密钥环境变量`：secret 只能来自环境变量
- `异常-禁裸except`：捕获要具体，并打印/返回错误
- `脚本-资源关闭`：连接用完必须关闭

另注：涉及 SQL 排版时写明「遵循 python-mysql-format」。

## 5. 建议文件结构

列出将生成的文件及职责。默认：

```text
practice/<topic>/
  WORKSHEET.md
  solution.py
```

## 6. 待你填充的槽位

对应代码文件中的函数签名约定。此处列出：

- 函数名与签名
- 输入 / 输出约定
- 边界（空值、失败时返回什么）

代码文件里只放：

```python
def foo(...) -> ...:
    """约定写在这里。"""
    # TODO: 按 WORKSHEET 步骤实现
    pass
```

## 7. 功能自测清单

- [ ] …
- [ ] …

## 8. 规范自检清单

- [ ] …
- [ ] …

## 9. 能力标签预告

本练习主要练：例如 HTTP 调用 / 异常处理 / 分页批处理 / 规范命名 …

## 10. 下一步

填充代码后回复「校验」。卡住回复「卡住了」。要对照实现回复「看答案」。
